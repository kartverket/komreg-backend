info:
  name: Alle Reguleringer
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/reguleringer"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
