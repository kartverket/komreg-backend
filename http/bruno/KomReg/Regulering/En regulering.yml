info:
  name: En regulering
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/reguleringer/vest_lofoten"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
