info:
  name: Endringer i en regulering
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/reguleringer/vest_lofoten/endringer"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
