info:
  name: Fordelingsparametre kommune
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/kommuner/1860/fordelingsparametre"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
