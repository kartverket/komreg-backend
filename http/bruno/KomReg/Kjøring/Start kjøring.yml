info:
  name: Start kjøring
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/run/vest_lofoten"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
