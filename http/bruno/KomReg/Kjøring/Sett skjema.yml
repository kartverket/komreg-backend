info:
  name: Sett skjema
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/skjema"
  body:
    type: json
    data: |-
      {
        "mottaker": "DB_MATRIKKEL_MOTTAKER2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
